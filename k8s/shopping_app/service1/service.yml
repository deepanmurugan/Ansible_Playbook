apiVersion: v1
kind: Service
metadata:
  name: shopping-app-service1
spec:
  selector:
    app: shopping-app
    task: service1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http
