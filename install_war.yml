- name: Install Consul | Install consul dependent libraries
  aws_s3:
    bucket: projcts-artifacts
    object: "{{jobname}}/{{branchname}}-{{buildno}}/jobs/{{branchname}}/{{buildno}}"
    dest: /tmp
    mode: get
